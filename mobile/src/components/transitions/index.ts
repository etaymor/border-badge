/**
 * Transition components barrel export
 *
 * Shared element wrapper components for smooth screen transitions.
 * These components enable morphing animations between screens using
 * react-native-screen-transitions.
 */

export {
  SharedCountryImage,
  SharedCountryPressable,
  getCountrySharedTag,
} from './SharedCountryImage';

export { SharedTripImage, SharedTripPressable, getTripSharedTag } from './SharedTripImage';

export { SharedEntryImage, SharedEntryPressable, getEntrySharedTag } from './SharedEntryImage';
