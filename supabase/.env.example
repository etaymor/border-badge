# Supabase Configuration
# Get these values from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

# Project URL (found in Project Settings > API)
SUPABASE_URL=https://your-project-id.supabase.co

# Public/Anon Key (safe to use in client apps, respects RLS)
SUPABASE_ANON_KEY=your-anon-key-here

# Service Role Key (bypasses RLS - NEVER expose in client apps)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# JWT Secret (found in Project Settings > API > JWT Settings)
# Used for verifying JWTs in the backend
SUPABASE_JWT_SECRET=your-jwt-secret-here
